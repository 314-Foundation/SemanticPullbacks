# Source code for the paper "Pulling Back the Curtain on Deep Networks"

Install torch environment according to the `requirements.txt` file.

Run the `evaluate_quantus.py` script to recreate the numerical results.